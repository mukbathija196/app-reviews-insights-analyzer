name: Weekly Pulse

on:
  workflow_dispatch:
    inputs:
      iso_week:
        description: "Optional ISO week (e.g. 2026-W16)"
        required: false

jobs:
  groww:
    concurrency:
      group: pulse-groww
      cancel-in-progress: false
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Sync dependencies
        run: uv sync --all-extras

      - name: Cache HuggingFace
        uses: actions/cache@v4
        with:
          path: ~/.cache/huggingface
          key: hf-${{ runner.os }}-${{ hashFiles('uv.lock') }}
          restore-keys: |
            hf-${{ runner.os }}-

      - name: Run weekly pulse (live MCP delivery in draft mode)
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        run: |
          if [ -n "${{ github.event.inputs.iso_week }}" ]; then
            uv run pulse run --product groww --iso-week "${{ github.event.inputs.iso_week }}" --email-mode draft
          else
            uv run pulse run --product groww --email-mode draft
          fi
